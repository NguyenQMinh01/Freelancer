<nav class="navbar has-shadow" style = "z-index: 5; margin-top:80px;">
    <div class="container">
        <div class = "navbar">
            <% @categories.each do |item| %>
            <%= link_to "#{item.name}", search_path(category: item.id), class: "navbar-item" %>
            <% end %>
        </div>
    </div>
</nav>