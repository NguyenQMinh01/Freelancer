<section class="section">
    <div class="container">
        <div class="form-container">
            <p class="title is-4 has-text-centered"> What Services Are You Looking For? </p>

                <%= form_for @request do |f|%>
                <div class="filed">
                    <label class="label">Title (*) </label>
                    <%= f.text_field :title , placeholder: "I request...", class: "input" %>
                </div>
                <div class="filed">
                    <label class="label">Desription (*) </label>
                    <%= f.text_area :desription , placeholder: "I'm looking for...", class: "teatarea" %>
                </div>
                <div class="filed">
                    <label class="label">Choose a category </label>
                    <div class="select is-fullwidth">
                        <%= f.select (:category_id, options_for_select(@categories.map))%>
                    </div>
                </div>
                <div class="filed">
                    <label class="label">Title (*) </label>
                    <%= f.text_field :title , placeholder: "I request...", class: "input" %>
                </div>
                <% end %>
         
        </div>
    </div>
</section>